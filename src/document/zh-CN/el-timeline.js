const attributes = [
  {
    name: 'reverse',
    description: '指定节点排序方向，默认为正序',
    type: 'boolean',
    value: '—',
    default: 'false'
  }
]

const document = { attributes }

module.exports = document
